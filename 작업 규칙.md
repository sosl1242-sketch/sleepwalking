# 작업 규칙

이 문서는 `sleepwalking` 소설 프로젝트를 GPT 또는 Codex와 함께 작업할 때 지켜야 할 운영 규칙을 정리한다.

`회의록.md`는 순수 회의 기록만 이어붙이는 문서로 두고, 문서 수정 권한, 회의록 운영 방식, 설정 반영 규칙 같은 작업 규칙은 이 문서에서 관리한다.

---

## 1. 문서 역할 구분

### `회의록.md`

- 실제 회의 내용만 기록한다.
- 새 회의는 문서 아래쪽에 계속 이어붙인다.
- 회의 중 나온 아이디어, 질문, 선택지, 보류 사항, 결정 사항을 시간순으로 남긴다.
- 회의록에 적힌 내용은 그 자체로 확정 설정이 아니다.
- 회의록은 GPT와 Codex가 프로젝트의 흐름과 논의 맥락을 이해하기 위한 장기 컨텍스트 역할을 한다.

### 설정 계열 문서

아래 문서는 사용자의 명시 허가가 있을 때만 수정한다.

- `README.md`
- `기획.md`
- `주제와 메시지.md`
- `설정.md`
- `플롯 구조.md`
- `장면 목록.md`
- `소설 초안.md`
- `문체 가이드.md`
- `리서치 TODO.md`
- `작업 체크리스트.md`
- 하네스 프롬프트 관련 문서

---

## 2. 기본 원칙

1. 회의 중 나온 아이디어는 바로 설정으로 확정하지 않는다.
2. 자유로운 의견 교환 중에는 아무 문서도 수정하지 않는다.
3. 사용자가 명시적으로 지시한 항목만 실제 문서에 반영한다.
4. 사용자가 단순히 아이디어를 말하거나 회의하자고 한 경우에는 실제 설정 문서, 초안 문서, 회의록 문서를 수정하지 않는다.
5. 애매한 경우 GPT는 수정하지 않고 먼저 사용자에게 확인한다.

---

## 3. 회의 중 문서 수정 금지 규칙

회의가 진행 중일 때는 GitHub에 아무것도 쓰지 않는다.

금지되는 작업:

- `회의록.md` 자동 수정
- `작업 규칙.md` 자동 수정
- `설정.md` 자동 수정
- `소설 초안.md` 자동 수정
- `기획.md`, `주제와 메시지.md`, `플롯 구조.md`, `장면 목록.md`, `문체 가이드.md` 등 다른 문서 자동 수정
- 회의 중 나온 아이디어를 확정 설정처럼 저장하는 행위
- 사용자가 아직 선택하지 않은 후보를 회의록에 기정사실처럼 기록하는 행위

회의 중에는 대화 안에서만 자유롭게 의견을 교환한다.

---

## 4. 회의록 작성 조건

회의록은 자동으로 작성하거나 업로드하지 않는다.

회의록 작성은 아래 순서를 모두 만족할 때만 진행한다.

1. 사용자가 “오늘 회의 끝, 정리해줘”처럼 회의 종료와 정리 요청을 명시한다.
2. GPT는 먼저 채팅 안에서 회의 정리본만 작성한다.
3. 사용자가 그 정리본을 확인한 뒤 “그 정리본을 회의록에 올려줘”라고 명시한다.
4. 그때만 `회의록.md`에 업로드한다.

즉, 회의 중에는 `회의록.md`를 수정하지 않는다.

---

## 5. 회의록 운영 규칙

- 회의록은 아래쪽으로 계속 이어붙이는 방식으로 관리한다.
- 새 회의, 새 결정, 새 보류 사항은 기존 내용을 덮어쓰지 않고 하단에 추가한다.
- 기존 회의록 내용을 정리하거나 삭제하거나 순서를 바꾸는 작업도 사용자의 허가가 있을 때만 한다.
- 회의록에 새 항목을 추가할 때는 사용자의 발언을 확정 설정처럼 쓰지 않는다.
- 가능한 한 `안건`, `논의 내용`, `결정된 내용`, `보류한 내용`, `반영 대기 항목`을 구분해서 기록한다.
- 단, 이 규칙은 사용자가 회의록 업로드를 명시적으로 허가한 경우에만 적용한다.

---

## 6. 문서 수정 권한 규칙

GPT 또는 Codex는 사용자의 명시 허가 없이 어떤 문서도 스스로 수정하지 않는다.

실제 문서 변경은 사용자가 다음과 같이 명시했을 때만 진행한다.

- “반영해줘”
- “추가해줘”
- “수정해줘”
- “옮겨줘”
- “회의록에 올려줘”
- “이번 회의에서 결정된 것만 설정에 넣어줘”
- “보류한 건 빼고 확정한 것만 문체 가이드에 넣어줘”

명시 지시가 없는 경우에는 다음처럼 처리한다.

- 대화 안에서만 의견을 제시한다.
- 회의록, 설정 문서, 원고 문서를 포함한 모든 문서를 건드리지 않는다.
- 필요한 경우 “이 내용을 실제 문서에 반영할까요?”라고 물어본다.

---

## 7. 반영 요청 처리 방식

사용자가 회의록의 내용을 실제 문서에 반영하라고 지시하면 다음 순서로 처리한다.

1. 반영할 회의 번호와 항목을 확인한다.
2. 보류 항목과 폐기 항목은 제외한다.
3. 사용자가 지정한 문서만 수정한다.
4. 수정 후 어떤 문서에 무엇을 반영했는지 짧게 보고한다.

---

## 8. 하네스 프롬프트 관련 규칙

- 하네스 프롬프트와 GPT 답변 제한 규칙은 아직 저장소에 추가하지 않는다.
- 사용자가 나중에 요청할 때 별도 문서로 추가한다.
- 그 전까지는 회의 중 언급하더라도 설정 문서나 초안 문서에 반영하지 않는다.

---

## 9. 현재 적용 상태

- `회의록.md`는 순수 회의 기록 문서로 둔다.
- `작업 규칙.md`는 문서 수정 권한과 운영 규칙을 관리한다.
- 자유로운 의견 교환 중에는 어떤 문서도 수정하지 않는다.
- 회의록은 사용자가 회의 종료 후 정리 요청을 하고, 이후 업로드를 명시적으로 허가했을 때만 수정한다.
- 앞으로 규칙 변경은 사용자가 명시적으로 지시한 경우에만 이 문서에 반영한다.
